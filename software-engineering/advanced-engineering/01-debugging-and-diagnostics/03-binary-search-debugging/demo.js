// Binary Search Debugging — tiny debugging simulation.
// Run: node demo.js

const situation = {
  topic: 'Binary Search Debugging',
  symptom: 'a regression introduced somewhere in a long pipeline',
  nextMove: 'Cut the search space in half repeatedly instead of wandering through the codebase.',
  lesson: 'The right next observation is usually worth more than a random code edit.'
};

console.log('topic:', situation.topic);
console.log('symptom:', situation.symptom);
console.log('next move:', situation.nextMove);
console.log('lesson:', situation.lesson);
