// Log Forensics — tiny debugging simulation.
// Run: node demo.js

const situation = {
  topic: 'Log Forensics',
  symptom: 'a burst of API failures mixed into normal request traffic',
  nextMove: 'Extract signal from noisy logs and reconstruct what actually happened.',
  lesson: 'The right next observation is usually worth more than a random code edit.'
};

console.log('topic:', situation.topic);
console.log('symptom:', situation.symptom);
console.log('next move:', situation.nextMove);
console.log('lesson:', situation.lesson);
