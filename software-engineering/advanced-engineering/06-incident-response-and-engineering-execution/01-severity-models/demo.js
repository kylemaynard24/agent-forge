// Severity Models — tiny incident execution simulation.
// Run: node demo.js

const incident = {
  topic: 'Severity Models',
  situation: 'an outage where every team member uses a different severity word',
  firstAction: 'Classify incidents by impact and urgency so the response matches reality.',
  note: 'Good execution makes facts, priorities, and next actions legible.'
};

console.log('topic:', incident.topic);
console.log('situation:', incident.situation);
console.log('first action:', incident.firstAction);
console.log('note:', incident.note);
