// Stakeholder Updates — tiny incident execution simulation.
// Run: node demo.js

const incident = {
  topic: 'Stakeholder Updates',
  situation: 'an incident affecting customers, support, and leadership differently',
  firstAction: 'Communicate status clearly to people who need truth, not implementation detail.',
  note: 'Good execution makes facts, priorities, and next actions legible.'
};

console.log('topic:', incident.topic);
console.log('situation:', incident.situation);
console.log('first action:', incident.firstAction);
console.log('note:', incident.note);
